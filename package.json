{
  "name": "taiwanagla2024",
  "version": "1.0.0",
  "description": "Taiwan Agla 2024 Website",
  "main": "index.html",
  "scripts": {
    "start": "echo 'No build command needed'",
    "build": "mkdir -p dist && cp index.html shop.js style.css dist/ && mkdir -p dist/netlify/functions && cp -r netlify/functions/* dist/netlify/functions/"
  },
  "dependencies": {
    "got": "^11.8.5"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "private": true
}
